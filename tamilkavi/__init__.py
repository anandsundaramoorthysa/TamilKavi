"""tamilkavi -- a command-line tool for exploring Tamil Kavithaigal."""

# Single source of truth for the version. setup.py reads this file, and the
# --version flag reports it, so the two can never drift apart.
__version__ = "0.8.0"
